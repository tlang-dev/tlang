expose add
expose greet

func add(a: Int, b: Int): Int {
    return a + b
}

func greet(name: String): String {
    return "Hello, " + name + "!"
}

func secret(): String {
    return "this is private"
}
