# Conformity test manifest — template include attrs
#
# Declares a registry dependency on the Kotlin generator from the local tbox.
# Run `tlang package generators/kotlin` first to publish it.

name: TmplIncludeAttrs
project: ConformityTests
organisation: TLang
version: 1.0.0
stability: alpha
releaseNumber: 1

dependencies:
  - TLangGen/KotlinGen/Kotlin 1.0.0:alpha:1 KotlinGen
