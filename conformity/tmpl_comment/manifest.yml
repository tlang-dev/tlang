# Conformity test manifest — template comment generation
#
# Tests that `comment text` directives inside lang blocks are correctly
# serialised as generated `// text` comments in the target language output.
#
# Depends on the Kotlin generator from the local tbox.
# Run `tlang package generators/kotlin` first to publish it.

name: TmplComment
project: ConformityTests
organisation: TLang
version: 1.0.0
stability: alpha
releaseNumber: 1

dependencies:
  - TLangGen/KotlinGen/Kotlin 1.0.0:alpha:1 KotlinGen
